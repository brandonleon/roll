[project]
name = "roll"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.13.1",
]

[dependency-groups]
dev = [
    "icecream>=2.1.3",
]

[project.scripts]
roll = "roll:app"